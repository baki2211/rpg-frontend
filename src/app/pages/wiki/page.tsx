'use client';

import { WikiBrowser } from '../../components/wiki/WikiBrowser';

const WikiPage: React.FC = () => {
  return (
    <div>
      <WikiBrowser />
    </div>
  );
};

export default WikiPage; 